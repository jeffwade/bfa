color randomColor(int hueLo, int hueHi) {
  colorMode(HSB, 360, 100, 100, 100);
  int rHue, rSat, rVal, rAlp; // random values for hue, saturation, and alpha
  // int[] hueRange = [hueLo, hueHi];
  return 0;
}